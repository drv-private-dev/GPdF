Final project: dynamic sections.json + per-section question files.
Edit data/sections.json to add/remove sections. Each section points to a JSON file in data/.
Run a static server (python -m http.server) from project folder and open http://localhost:8000
